unit: feat/fighter/assisting-shot
name: "_{Assisting Shot}"
group: "_{Core Rulebook}"

inc:
  - at: '@reference/action-traits'
    add:
      - press

  - copy: feat/fighter/assisting-shot
    contents:
      - article:
        title: "_{Assisting Shot}"
        cat: "_{Feat}"
        contents:
          - flags:
              - "_{Press}"
          - action:
            contents:
              - p: "_{Make an attack. If it hits, the next ally to attack that target gets [b]+1[/b]; if a critical, [b]+2[/b].}"
