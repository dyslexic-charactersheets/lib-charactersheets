unit: defs/calcs

inc:
  - copy: half-level
    params:
      id: ''
      label: "_{Class DC}"
    contents:
      - calc:
        output:
          field: '#{id}'
          frame: left
          width: large
          label: "#{label}"
        inputs:
          - field:
            ref: level
            label: "_{Level}"
          - span: "&divide; 2"
