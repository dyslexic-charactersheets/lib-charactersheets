unit: defs/calcs

inc:
  - copy: half-level
    params:
      id: ''
      label: "_{Class DC}"
      width: large
    contents:
      - calc:
        output:
          field: '#{id}'
          frame: left
          width: "#{width}"
          label: "#{label}"
        inputs:
          - field:
            ref: level
            label: "_{Level}"
          - span: "&divide; 2"
