unit: cantrip/vector-screen
name: "_{Vector Screen}"
group: "_{Dark Archive}"

inc:
  - copy: cantrip/vector-screen
    contents:
      - article: cantrip/vector-screen
        title: "_{Vector Screen}"
        cat: "_{Cantrip} 3"
        contents:
          - dl:
              range: "_{60ft}"
              area: "_{5ft square}"
              duration: "_{1rd}"

          - list:
            hr: true
            light: true
            contents:
              - action: 2
                contents:
                  - p: "_{Set up a transparent barrier that siezes small projectiles.}"
                  - value-block: "-2"
                    content: "_{Attack with larger ranged weapons.}"

              - level: 5
                marker: "_{Spell Level}"
                contents:
                  - dl:
                      area: "_{10ft}"

              - level: 6
                marker: "_{Spell Level}"
                contents:
                  - p: "_{Defends against magical blasts}"
                  - value-block: "-2"
                    content: "_{Spell attacks}"
                  - p: "_{Grants cover against ranged attacks}"
