unit: heritage/apg/tiefling
in: heritage/versatile
group: "_{Advanced Player's Guide}"
name: "_{Tiefling}"

require:
  - unit: optional-darkvision

inc:
  - set: heritage
    value: "_{Tiefling}"

  - at: '@heritage'
    replace:
      - article:
        title: "_{Tiefling}"
        contents:
          - flags:
              - "_{Tiefling}"
          - p: "_{May take tiefling feats.}"
          - paste: optional-darkvision
