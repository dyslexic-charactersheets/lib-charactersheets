unit: option/build
name: "_{Build A Character}"
group: "_{Core Rulebook}"

inc:
  - at: '@pages'
    add:
      - page: build
        name: "_{Build A Character}"
        no-bg: true
        order: 2
        flex: true
        numbered: false
        contents:

# ...
