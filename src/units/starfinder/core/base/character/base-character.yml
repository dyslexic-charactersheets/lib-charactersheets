unit: base/character
name: "_{Character}"
group: "_{Core Rulebook}"

form:
  - select: race
    name: "_{Race}"
    max: 1
  - select: theme
    name: "_{Theme}"
    max: 1
  - select: class
    name: "_{Class}"
  - select: archetype
    name: "_{Archetype}"

  - option: build
    name: "_{Build A Character}"
    unit: option/build
  - option: minis
    name: "_{Map Minis}"
    unit: option/minis

  # - select: inventory
  #   name: "_{Inventory}"
  #   max: 1
  #   default: half

inc:

  - at: '@pages'
    add:

      - page: character
        name: "_{Character}"
        order: 6
        flex: true
        contents:

      - page: combat
        name: "_{Combat}"
        order: 7
        flex: true
        contents:

