unit: heritage/elf/remaster/whisper
in: heritage/elf
group: "_{Player Core}"
name: "_{Whisper Elf}"

inc:
  - set: heritage
    value: "_{Whisper Elf}"
  - set: perception-darkvision
    value: true
    
  - at: '@heritage'
    replace:
      - article:
        title: "_{Whisper Elf}"
        contents:
          -p: "_{DC 3 to target concealed target.}"
          -p: "_{DC 9 to target hidden target.}"
          -p: "_{Must be able to hear target.}"
          - value-block: "_{+2}"
            content: "_{to detect creatures in a 30ft cone.}"
  
        
