unit: option/animal-companion/mount
name: "_{Mount}"
group: "_{Player Core}"

inc:

  - at: '@animal-companion/title'
    replace:
      - class-name: "_{Mount}"

  - set: animal-companion-speed-mount
    value: yes
